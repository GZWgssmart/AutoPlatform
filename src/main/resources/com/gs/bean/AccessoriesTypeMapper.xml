<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gs.dao.AccessoriesTypeDAO">

	<resultMap id="accessoriesTypeResultMap" type="accessoriesType">

	</resultMap>

	<insert id="insert"  parameterType="accessoriesType">
		<![CDATA[
			INSERT into t_accessories_type(accTypeId, accTypeName, accTypeDes, companyId, accTypeStatus) values(uuid(), #{accTypeName}, #{accTypeDes}, #{companyId}, #{accTypeStatus})
		]]>
	</insert>

	<insert id="batchInsert"  parameterType="list">
		<![CDATA[

		]]>
	</insert>

	<delete id="delete" parameterType="accessoriesType">
		<![CDATA[

		]]>
	</delete>

	<delete id="deleteById" parameterType="string">
		<![CDATA[

		]]>
	</delete>

	<delete id="batchDelete" parameterType="list">
		<![CDATA[

		]]>
	</delete>

	<update id="update" parameterType="accessoriesType">
		<![CDATA[
			update t_accessories_type set accTypeName = #{accTypeName}, accTypeDes = #{accTypeDes}, companyId = #{companyId}, accTypeStatus = #{accTypeStatus} where accTypeId = #{accTypeId}
		]]>
	</update>

	<update id="batchUpdate" parameterType="list">
		<![CDATA[

		]]>
	</update>

	<select id="queryAll" resultMap="accessoriesTypeResultMap">
		<![CDATA[

		]]>
	</select>

	<select id="queryByStatus" resultMap="accessoriesTypeResultMap" parameterType="string">
		<![CDATA[

		]]>
	</select>

	<select id="query" parameterType="accessoriesType" resultType="accessoriesType">
		<![CDATA[

		]]>
	</select>

	<select id="queryById" parameterType="string" resultType="accessoriesType">
		<![CDATA[

		]]>
	</select>

	<select id="queryByPager" resultType="list" resultMap="accessoriesTypeResultMap" parameterType="com.gs.common.bean.Pager">
		<![CDATA[
			select * from t_accessories_type limit #{beginIndex}, #{pageSize}
		]]>
	</select>

	<select id="queryByStatusPager" resultType="list" resultMap="accessoriesTypeResultMap">
		<![CDATA[
			select * from t_accessories_type where accTypeStatus = #{accTypeStatus} limit #{pager.beginIndex}, #{pager.pageSize}
		]]>
	</select>

	<select id="count" resultType="int">
	<![CDATA[
 			select count(accTypeId) from t_accessories_type
		]]>
</select>

	<select id="countByStatus" parameterType="String" resultType="int">
		<![CDATA[
			select count(*) from t_accessories_type where accTypeStatus = #{accTypeStatus}
		]]>
	</select>

	<update id="inactive" parameterType="string">
		<![CDATA[
			update t_accessories_type set accTypeStatus = 'N' where accTypeId = #{accTypeId}
		]]>
	</update>

	<update id="active" parameterType="string">
		<![CDATA[
			update t_accessories_type set accTypeStatus = 'Y' where accTypeId = #{accTypeId}
		]]>
	</update>

</mapper>
