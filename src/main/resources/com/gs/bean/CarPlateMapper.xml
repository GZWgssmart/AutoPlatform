<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gs.dao.CarPlateDAO">

	<resultMap id="carPlateResultMap" type="carPlate">

	</resultMap>

	<insert id="insert"  parameterType="carPlate">
		insert into t_car_plate (plateId,plateName,plateDes,plateStatus) values (UUID(),#{plateName},#{plateDes},'Y')
	</insert>

	<insert id="batchInsert"  parameterType="list">
		<![CDATA[

		]]>
	</insert>

	<delete id="delete" parameterType="carPlate">
		<![CDATA[

		]]>
	</delete>

	<delete id="deleteById" parameterType="string">
		<![CDATA[

		]]>
	</delete>

	<delete id="batchDelete" parameterType="list">
		<![CDATA[

		]]>
	</delete>

	<update id="update" parameterType="carPlate">
		update t_car_plate set plateName=#{plateName},plateDes=#{plateDes},plateStatus=#{plateStatus} where plateId = #{plateId}
	</update>

	<update id="batchUpdate" parameterType="list">
		<![CDATA[

		]]>
	</update>

	<select id="queryAll" resultMap="carPlateResultMap">
		select * from t_car_plate
	</select>

	<select id="queryByStatus" resultMap="carPlateResultMap" parameterType="string">
		<![CDATA[

		]]>
	</select>

	<select id="query" parameterType="carPlate" resultType="carPlate">
		<![CDATA[

		]]>
	</select>

	<select id="queryById" parameterType="string" resultType="carPlate">
		<![CDATA[

		]]>
	</select>

	<select id="queryByPager" resultType="list" resultMap="carPlateResultMap" parameterType="com.gs.common.bean.Pager">
		select * from t_car_plate  limit #{beginIndex}, #{pageSize}
	</select>

	<select id="count" resultType="int">
		<![CDATA[
		select count(plateId) from t_car_plate
		]]>
	</select>

	<update id="inactive" parameterType="string">
		<![CDATA[

		]]>
	</update>

	<update id="active" parameterType="string">
		<![CDATA[

		]]>
	</update>

</mapper>
