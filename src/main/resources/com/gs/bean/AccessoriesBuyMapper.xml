<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gs.dao.AccessoriesBuyDAO">

    <resultMap id="accessoriesBuyResultMap" type="accessoriesBuy">
        <association property="accessories" javaType="com.gs.bean.Accessories">
            <id column="accId" property="accessories"></id>
        </association>
    </resultMap>

    <insert id="insert" parameterType="accessoriesBuy">
		insert into
		auto_platform.t_accessories_buy(accBuyId, accId, accUnit, accBuyCount, accBuyPrice, accBuyTotal, accBuyDiscount, accBuyMoney, accBuyTime, accBuyCreatedTime, companyId, accBuyStatus)
		values
		(#{accBuyId},#{accId},#{accUnit},#{accBuyCount},#{accBuyPrice},#{accBuyTotal},#{accBuyDiscount},#{accBuyMoney},now(),now(),#{companyId},#{accBuyStatus})

	</insert>

    <insert id="batchInsert" parameterType="list">
		<![CDATA[

        ]]>
	</insert>

    <delete id="delete" parameterType="accessoriesBuy">
		<![CDATA[

        ]]>
	</delete>

    <delete id="deleteById" parameterType="string">
		<![CDATA[

        ]]>
	</delete>

    <delete id="batchDelete" parameterType="list">
		<![CDATA[

        ]]>
	</delete>

    <update id="update" parameterType="accessoriesBuy">
		<![CDATA[

        ]]>
	</update>

    <update id="batchUpdate" parameterType="list">
		<![CDATA[

        ]]>
	</update>

    <select id="queryAll" resultMap="accessoriesBuyResultMap">
		<![CDATA[

        ]]>
	</select>

    <select id="queryByStatus" resultMap="accessoriesBuyResultMap" parameterType="string">
		<![CDATA[

        ]]>
	</select>

    <select id="query" parameterType="accessoriesBuy" resultType="accessoriesBuy">
		<![CDATA[

        ]]>
	</select>

    <select id="queryById" parameterType="string" resultType="accessoriesBuy">
		<![CDATA[

        ]]>
	</select>

    <select id="queryByPager" resultType="list" resultMap="accessoriesBuyResultMap" parameterType="com.gs.common.bean.Pager">
		select * from auto_platform.t_accessories_buy limit #{beginIndex},#{pageSize}
    </select>

    <select id="count" resultType="int">
		select count(*) from auto_platform.t_accessories_buy
	</select>

    <update id="inactive" parameterType="string">
		<![CDATA[

        ]]>
	</update>

    <update id="active" parameterType="string">
		<![CDATA[

        ]]>
	</update>

</mapper>
