<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gs.dao.UserDAO">

	<resultMap id="userResultMap" type="user">

	</resultMap>

	<insert id="insert"  parameterType="user">
		<![CDATA[
			INSERT INTO t_user(userId, userName, userNickname, userEmail, userPwd, userGender, userAddress, userPhone, userIdentity) VALUES (uuid(), #{userName}, #{userNickname}, #{userEmail}, #{userPwd}, #{userGender}, #{userAddress}, #{userPhone}, #{userIdentity})
		]]>
	</insert>

	<insert id="batchInsert"  parameterType="list">
		<![CDATA[

		]]>
	</insert>

	<delete id="delete" parameterType="user">
		<![CDATA[

		]]>
	</delete>

	<delete id="deleteById" parameterType="string">
		<![CDATA[

		]]>
	</delete>

	<delete id="batchDelete" parameterType="list">
		<![CDATA[

		]]>
	</delete>

	<update id="update" parameterType="user">
		<![CDATA[
			UPDATE t_user SET userName = #{userName}, userNickname = #{userNickname}, userDes = #{userDes}, userPwd = #{userPwd}, userPhone = #{userPhone}, userAddress = #{userAddress} WHERE userId = #{userId}
		]]>
	</update>

	<update id="batchUpdate" parameterType="list">
		<![CDATA[

		]]>
	</update>

	<select id="queryAll" resultMap="userResultMap">
		<![CDATA[

		]]>
	</select>

	<select id="queryByStatus" resultMap="userResultMap" parameterType="string">
		<![CDATA[

		]]>
	</select>

	<select id="query" parameterType="user" resultType="user">
		<![CDATA[

		]]>
	</select>

	<select id="queryById" parameterType="string" resultType="user">
		<![CDATA[

		]]>
	</select>

	<select id="queryByPager" resultType="list" resultMap="userResultMap" parameterType="com.gs.common.bean.Pager">
		<![CDATA[
			SELECT * FROM t_user limit #{beginIndex}, #{pageSize}
		]]>
	</select>

	<select id="count" resultType="int">
		<![CDATA[
			SELECT COUNT(*) FROM t_user
		]]>
	</select>

	<update id="inactive" parameterType="string">
		<![CDATA[
			UPDATE t_user set userStatus = 'N' where userId = #{userId}
		]]>
	</update>

	<update id="active" parameterType="string">
		<![CDATA[
			UPDATE t_user set userStatus = 'Y' where userId = #{userId}
		]]>
	</update>

</mapper>
